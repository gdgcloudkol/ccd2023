export interface ProfileTicketData {
  email: string;
  ticketId: string;
  datePurchased: string;
  amount: string;
  townscriptData: any;
}

export interface TicketContent {
  imageSrc: string;
  imageAlt: string;
  title: string;
  description: string;
  btnText: string;
}